spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/psyco
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
  security:
    user:
      name: leo
      password: secret